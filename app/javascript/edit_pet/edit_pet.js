export const refreshImages = () => {
  // just hide this thing for now
  document.querySelector('')
  // get all images from the dataset
  const data = document.querySelector("#image-container").dataset;
  console.log(data.photos);
}