<div class="container">
  <div class="row">
    <div class="col-12 margin-top-header-height">
      <ul class="pets-list">
        <% @pets.each do |pet| %>
          <li class="ui-card pet-card">
            <%= pet.name %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>